server:
  servlet:
    session:
      cookie:
        secure: true
  tomcat:
    protocol-header: x-forwarded-proto

spring:
  application:
    name: pss
  datasource:
    username: sa
    password: famogo
  h2:
    console:
      enabled: true
      settings:
        web-allow-others: true
      path: /pssdb
  servlet:
    multipart:
      max-file-size: 3000MB
      max-request-size: 3000MB
---
spring:
  profiles: local
  datasource:
    url: jdbc:h2:c:/my/db/pss4
server:
  servlet:
    session:
      cookie:
        secure: false

---
spring:
  profiles: test
  datasource:
    url: jdbc:h2:~/db/pssTest
---
spring:
  profiles: home
  datasource:
    url: jdbc:h2:~/db/pssHome
